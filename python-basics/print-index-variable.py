# Define variables for the text, shift amount, and alphabet reference
text = 'Hello World'
shift = 3
alphabet = 'abcdefghijklmnopqrstuvwxyz'
# Find the index of the first character ('H') and store it
index = alphabet.find(text[0])
# Print the index to the console to verify the result (outputs -1)
print(index)
