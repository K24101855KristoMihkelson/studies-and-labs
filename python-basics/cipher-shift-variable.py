# Declare a string variable for the initial message
text = 'Hello World'
# Verify and print the data type of the 'text' variable
print(type(text))
# Declare an integer variable to set the shift value for the cipher algorithm
shift = 3
