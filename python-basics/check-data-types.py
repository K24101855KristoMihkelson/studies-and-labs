# Declare a string variable for the text
text = 'Hello World'
# Output the data type of the 'text' variable (returns <class 'str'>)
print(type(text))
# Declare an integer variable for the cipher shift
shift = 3
# Output the data type of the 'shift' variable (returns <class 'int'>)
print(type(shift))
