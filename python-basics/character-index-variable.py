# Define variables for text, shift, and alphabet
text = 'Hello World'
shift = 3
alphabet = 'abcdefghijklmnopqrstuvwxyz'
# Find the index of the first character and store it in the 'index' variable
# Storing the result allows us to use this number later in the calculation
index = alphabet.find(text[0])
