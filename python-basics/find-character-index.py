# Define the initial text, shift amount, and the alphabet reference string
text = 'Hello World'
shift = 3
alphabet = 'abcdefghijklmnopqrstuvwxyz'
# Use the .find() method to locate the index of a specific character ('z')
# This returns 25 (since indexing starts at 0, 'z' is at the 25th position)
alphabet.find('z')
