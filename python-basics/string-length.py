# Declare a string variable
text = 'Hello World'
# Use the len() function to get the total number of characters and print it
# Note: len() counts every single character, including spaces (outputs 11)
print(len(text))
