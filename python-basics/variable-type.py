# Declare a string variable
text = 'Hello World'
# Use the type() function inside print() to output the data type of the variable
# This helps in debugging to ensure the variable holds the expected type of data
print(type(text))
